#ifndef _LIBRTL_BASE_UTIL_H
#define _LIBRTL_BASE_UTIL_H

// Macro for marking the begining and end of a new namespace called `NAME`
#define LIBRTL_NAMESPACE_BEGIN(NAME) namespace NAME {
#define LIBRTL_NAMESPACE_END(NAME) }

#endif
